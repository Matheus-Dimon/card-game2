import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    alignItems: 'center',
    borderWidth: 1,
    borderColor: '#ccc',
    backgroundColor: '#f4f4f4'
  },
  image: {
    width: 100,
    height: 130,
    borderRadius: 8,
  },
  name: {
    fontWeight: 'bold',
  },
});