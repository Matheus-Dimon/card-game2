import { StyleSheet } from 'react-native';

export  const styles = StyleSheet.create({
  container: {
    marginBottom: 20,
  },
  status: {
    fontWeight: 'bold',
    marginBottom: 5,
  },
  section: {
    marginTop: 10,
    fontWeight: 'bold',
  },
});